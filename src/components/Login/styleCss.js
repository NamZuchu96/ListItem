import { StyleSheet } from "react-native";

const styleCSS = StyleSheet.create({
  input: {
    width: `80%`,
    height: 50,
    flexDirection: `row`,
    paddingStart: 10,
    borderWidth: 1,
    borderRadius: 20,
    marginBottom: 10,
  },

  login: {
    width: `20%`,
    borderRadius: 7,
    textAlign: "center",
    padding: 5,
    borderWidth: 1,
  },
});

export default styleCSS;
